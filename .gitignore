# Local-only docs / scratch (not pushed to GitHub)
ARCHITECTURE.local.md
FIXES.local.md
BRAINSTORM-tomorrow.md
BRAINSTORM*.md

node_modules/
dist/

# The Next.js OAuth app (next-app/) is now tracked in this repo.
# Its own next-app/.gitignore keeps secrets (.env.local) and build output (.next/, node_modules/) out.
.claude/worktrees/

# Local secrets / config and Firebase deploy cache (never commit)
.env
.env*.local
.firebase/

# Emulator run artifacts (npm run test:rules)
*-debug.log

.DS_Store
